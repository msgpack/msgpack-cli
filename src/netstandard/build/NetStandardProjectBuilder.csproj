<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="14.0" DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <Import Project="$(MSBuildExtensionsPath)\$(MSBuildToolsVersion)\Microsoft.Common.props" Condition="Exists('$(MSBuildExtensionsPath)\$(MSBuildToolsVersion)\Microsoft.Common.props')" />
  <PropertyGroup>
    <MinimumVisualStudioVersion>10.0</MinimumVisualStudioVersion>
    <Configuration Condition=" '$(Configuration)' == '' ">Debug</Configuration>
    <Platform Condition=" '$(Platform)' == '' ">AnyCPU</Platform>
    <ProjectGuid>{A1B322FC-AA39-4894-AFA8-AB427A3FBA09}</ProjectGuid>
    <OutputType>Library</OutputType>
    <AppDesignerFolder>Properties</AppDesignerFolder>
    <RootNamespace>NetStandardProjectBuilder</RootNamespace>
    <AssemblyName>NetStandardProjectBuilder</AssemblyName>
    <DefaultLanguage>ja-JP</DefaultLanguage>
    <FileAlignment>512</FileAlignment>
    <ProjectTypeGuids>{786C830F-07A1-408B-BD7F-6EE04809D6DB};{FAE04EC0-301F-11D3-BF4B-00C04F79EFBC}</ProjectTypeGuids>
    <TargetFrameworkProfile>Profile111</TargetFrameworkProfile>
    <TargetFrameworkVersion>v4.5</TargetFrameworkVersion>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Debug|AnyCPU' ">
    <DebugSymbols>true</DebugSymbols>
    <DebugType>full</DebugType>
    <Optimize>false</Optimize>
    <OutputPath>bin\Debug\</OutputPath>
    <DefineConstants>DEBUG;TRACE</DefineConstants>
    <ErrorReport>prompt</ErrorReport>
    <WarningLevel>4</WarningLevel>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Release|AnyCPU' ">
    <DebugType>pdbonly</DebugType>
    <Optimize>true</Optimize>
    <OutputPath>bin\Release\</OutputPath>
    <DefineConstants>TRACE</DefineConstants>
    <ErrorReport>prompt</ErrorReport>
    <WarningLevel>4</WarningLevel>
  </PropertyGroup>
  <ItemGroup>
    <!-- A reference to the entire .NET Framework is automatically included -->
  </ItemGroup>
  <ItemGroup>
    <Compile Include="Properties\AssemblyInfo.cs" />
  </ItemGroup>
  <Import Project="$(MSBuildExtensionsPath32)\Microsoft\Portable\$(TargetFrameworkVersion)\Microsoft.Portable.CSharp.targets" />
  <PropertyGroup>
    <PreBuildEvent>echo Retore netstandard 1.1 project...
dotnet restore $(ProjectDir)..\1.1\MsgPack\project.json
if not %25ERRORLEVEL%25==0 (
  echo "Failed to restore netstandard1.1"
  exit 1
)

echo Build netstandard 1.1 project...
dotnet build $(ProjectDir)..\1.1\MsgPack\project.json -c $(ConfigurationName)
if not %25ERRORLEVEL%25==0 (
  echo "Failed to build netstandard1.1"
  exit 1
)

echo Retore netstandard 1.3 project...
dotnet restore $(ProjectDir)..\1.3\MsgPack\project.json
if not %25ERRORLEVEL%25==0 (
  echo "Failed to restore netstandard1.3"
  exit 1
)

echo Build netstandard 1.1 project...
dotnet build $(ProjectDir)..\1.3\MsgPack\project.json -c $(ConfigurationName)
if not %25ERRORLEVEL%25==0 (
  echo "Failed to build netstandard1.3"
  exit 1
)

if "Release"=="$(ConfigurationName)" (
echo Copy release binaries...
robocopy ..\1.1\bin\Release\ $(SolutionDir)bin\ /S
robocopy ..\1.3\bin\Release\ $(SolutionDir)bin\ /S
)</PreBuildEvent>
  </PropertyGroup>
  <!-- To modify your build process, add your task inside one of the targets below and uncomment it. 
       Other similar extension points exist, see Microsoft.Common.targets.
  <Target Name="BeforeBuild">
  </Target>
  <Target Name="AfterBuild">
  </Target>
  -->
</Project>